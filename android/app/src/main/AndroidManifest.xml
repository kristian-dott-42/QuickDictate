<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Send audio to the STT/LLM endpoints. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Capture the dictation. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- Draw the floating mic button over other apps. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- Keep recording reliable while the button is active. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <!-- Foreground-service notification on Android 13+. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_mic"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuickDictate">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Hosts the floating mic button + the record→transcribe→cleanup pipeline. -->
        <service
            android:name=".OverlayService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <!-- Inserts the cleaned text into whatever field currently has focus. -->
        <service
            android:name=".DictationAccessibilityService"
            android:exported="false"
            android:label="@string/accessibility_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

    </application>
</manifest>
